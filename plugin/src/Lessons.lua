-- No built-in lessons. The plugin fetches lessons from repos the user
-- configures in the Manage view. If you want a zero-config default,
-- preload an entry here — but keep in mind it duplicates whatever the
-- user later fetches from an upstream copy.
return {}
