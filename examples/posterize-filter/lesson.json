{
  "schemaVersion": 1,
  "id": "posterize-filter",
  "title": "Posterize filter",
  "goal": "Animate a radial ripple then quantize channels to 4 levels.",
  "tags": [
    "editableimage",
    "graphics",
    "filters"
  ],
  "steps": [
    {
      "id": "s1-intro",
      "type": "narrative",
      "body": "Posterizing snaps each color channel to the nearest step in an\nN-level ramp. With N=4 you get 64 possible colors total \u2014 the\nclassic silkscreen / retro-print look. We apply it each frame to\nan animated radial ripple.\n"
    },
    {
      "id": "s2-gui",
      "type": "scripted",
      "body": "ScreenGui host.",
      "action": {
        "op": "createInstance",
        "class": "ScreenGui",
        "parent": "StarterGui",
        "props": {
          "Name": "PosterizeGui",
          "ResetOnSpawn": false
        }
      },
      "focus": {
        "selection": "StarterGui.PosterizeGui"
      }
    },
    {
      "id": "s3-canvas",
      "type": "scripted",
      "body": "Canvas.",
      "action": {
        "op": "createInstance",
        "class": "ImageLabel",
        "parent": "StarterGui.PosterizeGui",
        "props": {
          "Name": "Canvas",
          "Size": [
            0,
            512,
            0,
            512
          ],
          "Position": [
            0.5,
            -256,
            0.5,
            -256
          ],
          "BackgroundColor3": [
            0,
            0,
            0
          ],
          "BorderSizePixel": 0
        }
      },
      "focus": {
        "selection": "StarterGui.PosterizeGui.Canvas"
      }
    },
    {
      "id": "s4-filter",
      "type": "codeEdit",
      "body": "Each pixel gets three channel-offset sines of the radial distance.\n`posterize(v)` rounds to the nearest of `LEVELS` steps, producing\nflat color bands.\n",
      "target": {
        "path": "StarterGui.PosterizeGui.Canvas.Posterize",
        "create": true,
        "class": "LocalScript"
      },
      "source": "local AssetService = game:GetService(\"AssetService\")\nlocal RunService = game:GetService(\"RunService\")\n\nlocal W, H = 256, 256\nlocal LEVELS = 4\n\nlocal label = script.Parent\nlocal img = AssetService:CreateEditableImage({ Size = Vector2.new(W, H) })\nlocal buf = buffer.create(W * H * 4)\n\nlocal function posterize(v)\n  return math.floor(v / 255 * (LEVELS - 1) + 0.5) / (LEVELS - 1) * 255\nend\n\nlocal t = 0\nRunService.Heartbeat:Connect(function(dt)\n  t += dt\n  local k = 0\n  for y = 0, H - 1 do\n    for x = 0, W - 1 do\n      local fx, fy = x / W - 0.5, y / H - 0.5\n      local d = math.sqrt(fx * fx + fy * fy)\n      local r = (math.sin(d * 20 - t * 2) * 0.5 + 0.5) * 255\n      local g = (math.sin(d * 20 - t * 2 + 2) * 0.5 + 0.5) * 255\n      local b = (math.sin(d * 20 - t * 2 + 4) * 0.5 + 0.5) * 255\n      buffer.writeu8(buf, k, math.floor(posterize(r)))\n      buffer.writeu8(buf, k + 1, math.floor(posterize(g)))\n      buffer.writeu8(buf, k + 2, math.floor(posterize(b)))\n      buffer.writeu8(buf, k + 3, 255)\n      k += 4\n    end\n  end\n  img:WritePixelsBuffer(Vector2.zero, img.Size, buf)\nend)\n\nlabel.ImageContent = Content.fromObject(img)\n",
      "focus": {
        "script": {
          "path": "StarterGui.PosterizeGui.Canvas.Posterize"
        }
      }
    },
    {
      "id": "s5-playtest",
      "type": "narrative",
      "body": "Press \u25b6 **Play** (F5). Concentric animated bands in chunky flat\ncolors. Bump `LEVELS` to 2 for two-tone, 16 for smooth.\n"
    }
  ]
}