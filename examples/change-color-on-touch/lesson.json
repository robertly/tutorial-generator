{
  "schemaVersion": 1,
  "id": "change-color-on-touch",
  "title": "Change Part Color On Touch",
  "goal": "Demonstrates changing the color of a part upon being touched by a player's character. The color change is debounced to ensure the Touched event does not trigger a color change too quickly. The part's color is randomly set using math.random.",
  "tags": [
    "Player & Character",
    "ported"
  ],
  "steps": [
    {
      "id": "s1-intro",
      "type": "narrative",
      "body": "Demonstrates changing the color of a part upon being touched by a player's character. The color change is debounced to ensure the Touched event does not trigger a color change too quickly. The part's color is randomly set using math.random.\n\nThis sample was ported from `solutions-samples/change-color-on-touch`. Every step is deterministic \u2014 watch the components assemble under `Workspace.ChangeColorOnTouch`."
    },
    {
      "id": "s2-folder-changecolorontouch",
      "type": "scripted",
      "body": "Add a Folder `ChangeColorOnTouch` under `Workspace` to hold this sample.",
      "action": {
        "op": "createInstance",
        "class": "Folder",
        "parent": "Workspace",
        "props": {
          "Name": "ChangeColorOnTouch"
        }
      },
      "focus": {
        "selection": "Workspace.ChangeColorOnTouch"
      }
    },
    {
      "id": "s3-clone-part",
      "type": "scripted",
      "body": "Clone the bundled `Part` instance tree into `Workspace.ChangeColorOnTouch`.",
      "action": {
        "op": "cloneFromAssets",
        "source": "ReplicatedStorage.TutorialAssets.change-color-on-touch.Part",
        "parent": "Workspace.ChangeColorOnTouch",
        "name": "Part"
      },
      "focus": {
        "selection": "Workspace.ChangeColorOnTouch.Part"
      }
    },
    {
      "id": "s4-changecolorontouchscript",
      "type": "codeEdit",
      "body": "Add `ChangeColorOnTouchScript` (Script) under `Workspace.ChangeColorOnTouch`.",
      "target": {
        "path": "Workspace.ChangeColorOnTouch.ChangeColorOnTouchScript",
        "create": true,
        "class": "Script"
      },
      "source": "--[[\n\tDemonstrates changing the color of a part upon being touched by a player's character.\n\tThe color change is debounced to ensure the Touched event does not trigger a color change too quickly.\n\tThe part's color is randomly set using math.random.\n--]]\n\nlocal Players = game:GetService(\"Players\")\n\nlocal part = script.Parent.Part\nlocal DEBOUNCE_TIME = 1\nlocal random = Random.new()\nlocal enabled = true\n\nlocal function onPartTouched(otherPart: BasePart)\n\tif enabled then\n\t\t-- Check if a player touched the part\n\t\tlocal character = otherPart.Parent\n\t\tlocal player = Players:GetPlayerFromCharacter(character)\n\t\tif player then\n\t\t\t-- Temporarily disable changing the Part's color\n\t\t\tenabled = false\n\n\t\t\t-- Set the Part to a random color\n\t\t\tpart.Color = Color3.new(random:NextNumber(), random:NextNumber(), random:NextNumber())\n\n\t\t\t-- Wait some time before reenabling\n\t\t\ttask.wait(DEBOUNCE_TIME)\n\n\t\t\t-- Enable changing the Part's color\n\t\t\tenabled = true\n\t\tend\n\tend\nend\n\n-- Enable changing the Part's color initially\npart.Touched:Connect(onPartTouched)\n",
      "focus": {
        "script": {
          "path": "Workspace.ChangeColorOnTouch.ChangeColorOnTouchScript"
        }
      }
    },
    {
      "id": "s5-playtest",
      "type": "narrative",
      "body": "Press \u25b6 Play (or F5) to see the sample run in-place. Shift+F5 to stop. Inspect `Workspace.ChangeColorOnTouch` in the Explorer to see the assembled result."
    }
  ],
  "assets": [
    {
      "kind": "model",
      "source": "ReplicatedStorage.TutorialAssets.change-color-on-touch.Part",
      "name": "Part",
      "description": "Bundled instance tree from solutions-samples/change-color-on-touch."
    }
  ]
}
